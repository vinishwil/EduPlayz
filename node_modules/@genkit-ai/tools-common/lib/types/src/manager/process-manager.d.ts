export type ProcessStatus = 'running' | 'stopped' | 'unconfigured';
export interface AppProcessStatus {
    status: ProcessStatus;
}
export interface ProcessManagerStartOptions {
    nonInteractive?: boolean;
}
export declare class ProcessManager {
    private readonly command;
    private readonly args;
    private readonly env;
    private appProcess?;
    private originalStdIn?;
    private _status;
    private manualRestart;
    constructor(command: string, args: string[], env?: NodeJS.ProcessEnv);
    start(options?: ProcessManagerStartOptions): Promise<void>;
    restart(options?: ProcessManagerStartOptions): Promise<void>;
    kill(): Promise<void>;
    status(): AppProcessStatus;
    private cleanup;
}
